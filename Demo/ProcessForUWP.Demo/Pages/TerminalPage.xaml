<Page
    x:Class="ProcessForUWP.Demo.Pages.TerminalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:ProcessForUWP.Demo.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:TerminalViewModel}"
    DataContext="{x:Bind Provider}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer
            Padding="4"
            HorizontalScrollBarVisibility="Auto"
            HorizontalScrollMode="Auto"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <RichTextBlock
                x:Name="Message"
                FontFamily="{ThemeResource CodeThemeFontFamily}"
                IsTextSelectionEnabled="True" />
        </ScrollViewer>
        <Grid Grid.Row="2" Padding="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="Input" VerticalAlignment="Center" />
            <Button
                Grid.Column="2"
                VerticalAlignment="Stretch"
                Click="Button_Click"
                Content="&#xE724;"
                FontFamily="{ThemeResource SymbolThemeFontFamily}">
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Enter" />
                </Button.KeyboardAccelerators>
            </Button>
        </Grid>
    </Grid>
</Page>
